/**
 * Merged ApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { object, optional, Schema, string } from '../schema';

/** Customizes the payer experience during the 3DS Approval for payment. */
export interface CardExperienceContext {
  /** The URL where the customer will be redirected upon successfully completing the 3DS challenge. */
  returnUrl?: string;
  /** The URL where the customer will be redirected upon cancelling the 3DS challenge. */
  cancelUrl?: string;
}

export const cardExperienceContextSchema: Schema<CardExperienceContext> = object(
  {
    returnUrl: ['return_url', optional(string())],
    cancelUrl: ['cancel_url', optional(string())],
  }
);
