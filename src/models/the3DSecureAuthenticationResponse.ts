/**
 * Merged ApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { object, optional, Schema } from '../schema';
import { EnrolledEnum, enrolledEnumSchema } from './enrolledEnum';
import { ParesStatusEnum, paresStatusEnumSchema } from './paresStatusEnum';

/** Results of 3D Secure Authentication. */
export interface The3DSecureAuthenticationResponse {
  /** The outcome of the issuer's authentication. */
  authenticationStatus?: ParesStatusEnum;
  /** Status of authentication eligibility. */
  enrollmentStatus?: EnrolledEnum;
}

export const the3DSecureAuthenticationResponseSchema: Schema<The3DSecureAuthenticationResponse> = object(
  {
    authenticationStatus: [
      'authentication_status',
      optional(paresStatusEnumSchema),
    ],
    enrollmentStatus: ['enrollment_status', optional(enrolledEnumSchema)],
  }
);
