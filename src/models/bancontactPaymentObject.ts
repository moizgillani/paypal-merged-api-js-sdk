/**
 * Paypal Merged APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { object, optional, Schema, string, unknown } from '../schema';

/** Information used to pay Bancontact. */
export interface BancontactPaymentObject {
  /** The name of the account holder associated with this payment method. */
  name?: string;
  /** The [two-character ISO-3166-1 country code](/docs/integration/direct/rest/country-codes/) of the bank. */
  countryCode?: string;
  /** The bank identification code (BIC). */
  bic?: string;
  /** The last characters of the IBAN used to pay. */
  ibanLastChars?: string;
  /** The last digits of the card used to fund the Bancontact payment. */
  cardLastDigits?: string;
  /** Attributes for SEPA direct debit object. */
  attributes?: unknown;
}

export const bancontactPaymentObjectSchema: Schema<BancontactPaymentObject> = object(
  {
    name: ['name', optional(string())],
    countryCode: ['country_code', optional(string())],
    bic: ['bic', optional(string())],
    ibanLastChars: ['iban_last_chars', optional(string())],
    cardLastDigits: ['card_last_digits', optional(string())],
    attributes: ['attributes', optional(unknown())],
  }
);
