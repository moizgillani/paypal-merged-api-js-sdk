/**
 * Paypal Merged APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string, unknown } from '../schema';
import { CardBrandEnum, cardBrandEnumSchema } from './cardBrandEnum';
import {
  CardVerificationDetails,
  cardVerificationDetailsSchema,
} from './cardVerificationDetails';

/** Full representation of a Card Payment Token. */
export interface CardResponsePaymentMethodTokens {
  /** The card holder's name as it appears on the card. */
  name?: string;
  /** The last digits of the payment card. */
  lastDigits?: string;
  /** The card brand or network. Typically used in the response. */
  brand?: CardBrandEnum;
  /** The card expiration year and month, in [Internet date format](https://tools.ietf.org/html/rfc3339#section-5.6). */
  expiry?: string;
  /** The billing address for this card. Supports only the `address_line_1`, `address_line_2`, `admin_area_1`, `admin_area_2`, `postal_code`, and `country_code` properties. */
  billingAddress?: unknown;
  /** Card Verification status. */
  verificationStatus?: string;
  /** Card Verification details including the authorization details and 3D SECURE details. */
  verification?: CardVerificationDetails;
}

export const cardResponsePaymentMethodTokensSchema: Schema<CardResponsePaymentMethodTokens> = object(
  {
    name: ['name', optional(string())],
    lastDigits: ['last_digits', optional(string())],
    brand: ['brand', optional(cardBrandEnumSchema)],
    expiry: ['expiry', optional(string())],
    billingAddress: ['billing_address', optional(unknown())],
    verificationStatus: ['verification_status', optional(string())],
    verification: [
      'verification',
      optional(lazy(() => cardVerificationDetailsSchema)),
    ],
  }
);
