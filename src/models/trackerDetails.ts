/**
 * Merged ApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  object,
  optional,
  Schema,
  string,
  unknown,
} from '../schema';
import { LinkDescription, linkDescriptionSchema } from './linkDescription';
import { TrackerItem, trackerItemSchema } from './trackerItem';

export interface TrackerDetails {
  /** The tracker id. */
  id?: string;
  status?: unknown;
  /** An array of details of items in the shipment. */
  items?: TrackerItem[];
  /** An array of request-related HATEOAS links. */
  links?: LinkDescription[];
}

export const trackerDetailsSchema: Schema<TrackerDetails> = object({
  id: ['id', optional(string())],
  status: ['status', optional(unknown())],
  items: ['items', optional(array(lazy(() => trackerItemSchema)))],
  links: ['links', optional(array(lazy(() => linkDescriptionSchema)))],
});
