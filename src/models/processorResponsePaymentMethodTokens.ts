/**
 * Merged ApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { object, optional, Schema } from '../schema';
import { AVSCodeEnum, aVSCodeEnumSchema } from './aVSCodeEnum';
import { CVVCodeEnum, cVVCodeEnumSchema } from './cVVCodeEnum';
import {
  PaymentAdviceCodeEnum,
  paymentAdviceCodeEnumSchema,
} from './paymentAdviceCodeEnum';
import {
  ProcessorResponseResponseCodeEnum,
  processorResponseResponseCodeEnumSchema,
} from './processorResponseResponseCodeEnum';

/** The processor information. Might be required for payment requests, such as direct credit card transactions. */
export interface ProcessorResponsePaymentMethodTokens {
  /** The address verification code for Visa, Discover, Mastercard, or American Express transactions. */
  avsCode?: AVSCodeEnum;
  /** The card verification value code for for Visa, Discover, Mastercard, or American Express. */
  cvvCode?: CVVCodeEnum;
  /** Processor response code for the non-PayPal payment processor errors. */
  responseCode?: ProcessorResponseResponseCodeEnum;
  /** The declined payment transactions might have payment advice codes. The card networks, like Visa and Mastercard, return payment advice codes. */
  paymentAdviceCode?: PaymentAdviceCodeEnum;
}

export const processorResponsePaymentMethodTokensSchema: Schema<ProcessorResponsePaymentMethodTokens> = object(
  {
    avsCode: ['avs_code', optional(aVSCodeEnumSchema)],
    cvvCode: ['cvv_code', optional(cVVCodeEnumSchema)],
    responseCode: [
      'response_code',
      optional(processorResponseResponseCodeEnumSchema),
    ],
    paymentAdviceCode: [
      'payment_advice_code',
      optional(paymentAdviceCodeEnumSchema),
    ],
  }
);
