/**
 * Paypal Merged APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string, unknown } from '../schema';
import { ExperienceContext, experienceContextSchema } from './experienceContext';

/** Information needed to pay using iDEAL. */
export interface TheIDEALPaymentInformation {
  /** The name of the account holder associated with this payment method. */
  name: string;
  /** The [two-character ISO-3166-1 country code](/docs/integration/direct/rest/country-codes/) of the bank. */
  countryCode: string;
  /** The bank identification code (BIC). */
  bic?: string;
  /** Customizes the payer experience during the approval process for the payment. */
  experienceContext?: ExperienceContext;
  /** Attributes for altpay recurring. */
  attributes?: unknown;
}

export const theIDEALPaymentInformationSchema: Schema<TheIDEALPaymentInformation> = object(
  {
    name: ['name', string()],
    countryCode: ['country_code', string()],
    bic: ['bic', optional(string())],
    experienceContext: [
      'experience_context',
      optional(lazy(() => experienceContextSchema)),
    ],
    attributes: ['attributes', optional(unknown())],
  }
);
