/**
 * Paypal Merged APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { boolean, lazy, object, optional, Schema, string } from '../schema';
import {
  BillingAgreementPatternEnum,
  billingAgreementPatternEnumSchema,
} from './billingAgreementPatternEnum';
import { CustomerTypeEnum, customerTypeEnumSchema } from './customerTypeEnum';
import { ShippingDetails, shippingDetailsSchema } from './shippingDetails';
import { UsageTypeEnum, usageTypeEnumSchema } from './usageTypeEnum';

export interface PaypalWalletBaseDetails {
  /** The description displayed to PayPal consumer on the approval flow for PayPal, as well as on the PayPal payment token management experience on PayPal.com. */
  description?: string;
  /** Expected business/pricing model for the billing agreement. */
  usagePattern?: BillingAgreementPatternEnum;
  /** The shipping address for the Payer. */
  shipping?: ShippingDetails;
  /** The usage type associated with the PayPal payment token. */
  usageType?: UsageTypeEnum;
  /** The customer type associated with the PayPal payment token. This is to indicate whether the customer acting on the merchant / platform is either a business or a consumer. */
  customerType?: CustomerTypeEnum;
  /** Create multiple payment tokens for the same payer, merchant/platform combination. Use this when the customer has not logged in at merchant/platform. The payment token thus generated, can then also be used to create the customer account at merchant/platform. Use this also when multiple payment tokens are required for the same payer, different customer at merchant/platform. This helps to identify customers distinctly even though they may share the same PayPal account. This only applies to PayPal payment source. */
  permitMultiplePaymentTokens?: boolean;
}

export const paypalWalletBaseDetailsSchema: Schema<PaypalWalletBaseDetails> = object(
  {
    description: ['description', optional(string())],
    usagePattern: [
      'usage_pattern',
      optional(billingAgreementPatternEnumSchema),
    ],
    shipping: ['shipping', optional(lazy(() => shippingDetailsSchema))],
    usageType: ['usage_type', optional(usageTypeEnumSchema)],
    customerType: ['customer_type', optional(customerTypeEnumSchema)],
    permitMultiplePaymentTokens: [
      'permit_multiple_payment_tokens',
      optional(boolean()),
    ],
  }
);
